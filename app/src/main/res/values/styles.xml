<resources>

    <!-- Tema Rosado -->
    <style name="Theme.PinkLight" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/pink_light</item>
        <item name="colorPrimaryDark">@color/pink_light</item>
        <item name="colorAccent">@color/secondary_text</item>
    </style>

    <!-- Tema Verde Claro -->
    <style name="Theme.GreenLight" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green_light</item>
        <item name="colorPrimaryDark">@color/green_light</item>
        <item name="colorAccent">@color/secondary_text</item>
    </style>

    <!-- Tema Morado Claro -->
    <style name="Theme.BlueLight" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_light</item>
        <item name="colorPrimaryDark">@color/purple_light</item>
        <item name="colorAccent">@color/secondary_text</item>
    </style>

    <!-- Tema por defecto (puedes modificarlo si lo deseas) -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/fondo</item>
        <item name="colorPrimaryDark">@color/dark_primary</item>
        <item name="colorAccent">@color/accent</item>
    </style>

</resources>
